{
  "name": "ai-orchestrator-functions",
  "version": "2.0.0",
  "description": "Serverless AI Orchestrator for Math Education Platform",
  "main": "index.js",
  "scripts": {
    "start": "functions-framework --target=orchestrateAI",
    "deploy": "gcloud functions deploy ai-orchestrator --runtime nodejs20 --trigger-http --allow-unauthenticated",
    "test": "jest",
    "lint": "eslint ."
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.3.0",
    "@google-cloud/firestore": "^7.1.0",
    "@google-cloud/pubsub": "^4.0.0",
    "@google-cloud/secret-manager": "^5.0.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.50.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}